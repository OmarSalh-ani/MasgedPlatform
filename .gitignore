# Secrets — never commit
.env
**/.env

# Google service account private keys — provide per deployment, never commit
**/firebase-service-account.json

# Real local credentials live here; committed appsettings.json holds placeholders
**/appsettings.Development.json

# Node
**/node_modules/
**/dist/
**/build/

# .NET
**/bin/
**/obj/
**/publish/
AdminAPI/_buildcheck/

# Runtime data — lives on the deployment server, not in source control
AdminAPI/Uploads/
AdminAPI/FilesManager/
AdminAPI/Logs/
MasgedParentMobileAPI/Uploads/

# Flutter app lives in its own repo: OmarSalh-ani/Masged-Mubarak-Flutter
ParentApp/

# Flutter / Dart
.dart_tool/
**/.dart_tool/
**/.flutter-plugins
**/.flutter-plugins-dependencies

# IDE / OS
.vs/
.idea/
*.user
Thumbs.db
.DS_Store

_publish-staging/
